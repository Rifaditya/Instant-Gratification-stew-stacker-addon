# ⚙️ Configuration & GameRules (Minecraft 26.2)

> 📌 **Repository Source Disclaimer**: The documentation in this Wiki reflects the **current source code state in the repository**, which may include recent unreleased commits or developmental features ahead of public release builds on CurseForge and Modrinth.

## 1. Official Infobox
| Parameter | Technical Details |
| :--- | :--- |
| **Subsystem Name** | Configuration & Dynamic GameRule System |
| **Config File** | `.minecraft/config/stew-stacker.json` |
| **Config Version** | `1` (`public static final int VERSION = 1;`) |
| **Config Manager** | `StewStackerConfig.java` (backed by `ConfigHelper`) |
| **GUI Provider** | YetAnotherConfigLib v3 (`YaclScreenHelper.java`) |
| **ModMenu Bridge** | `ModMenuIntegration.java` (`GuiHelper.getOptionalYaclFactory`) |
| **GameRule Category** | `stew-stacker-addon:stew-stacker-addon` |
| **Player Freedom Range** | $1 \dots 2{,}147{,}483{,}647$ (`Integer.MAX_VALUE`) |
| **Warning Threshold** | Values $> 39{,}768{,}215$ trigger safe ceiling warning |

---

## 2. Step-by-Step Player & Administrator Workflow

1. **Editing Global Defaults (Title Screen)**:
   * Navigate to **Mods** -> **Stew Stacker Addon** -> **Configure** (or click the gear icon).
   * The YACL interface allows configuring `Stew & Soup Limit`.
   * Global configuration changes saved here define the initial default GameRules for **newly created worlds**.

2. **Configuring Active Worlds via Commands**:
   * To change limits in an existing singleplayer world or dedicated server, run:
     `/gamerule stew-stacker-addon:stew_limit <value>`
   * Changes apply immediately without server restart.

3. **In-Game GameRules GUI**:
   * When creating or editing a world, click **Edit Game Rules**.
   * Scroll to the **Stew Stacker Addon** category.
   * Mutate values directly with full integer validation.

---

## 3. Mathematical Formulas & Range Governance

### The Player Agency & Anti-Nanny Invariant
In accordance with our core design constitution, Stew Stacker Addon never imposes artificial gameplay ceilings or restrictive arbitrary caps. The valid mathematical domain is:
$$D = \{ x \in \mathbb{Z} \mid 1 \le x \le 2^{31}-1 \}$$

### Large Chest Capacity & Integer Overflow Math
A double chest consists of 54 slots. The safe ceiling $S_{\text{safe}}$ before total container item count overflows a signed 32-bit integer is:
$$S_{\text{safe}} = \left\lfloor \frac{2^{31}-1}{54} \right\rfloor = 39{,}768{,}215$$

When an administrator inputs a value $v > 39{,}768{,}215$, YACL displays the contextual warning:
$$\text{Warning}(v): \text{"⚠️ Large stack counts can cause container integer overflows!"}$$
However, players retain full liberty to proceed if desired.

---

## 4. Visual ASCII Configuration Flowchart

```
   [ User Edits Config in Main Menu ]
                   |
                   v
   Saves to .minecraft/config/stew-stacker.json
                   |
         +---------+---------+
         |                   |
         v                   v
  [ New World Created ]  [ Existing World Loaded ]
         |                   |
   isInitialized = false     isInitialized = true
         |                   |
   Apply config values       Load saved GameRules from
   directly to GameRules     level.dat (Config does NOT overwrite)
         |                   |
         +---------+---------+
                   |
                   v
         [ In-Game World Active ]
                   |
                   v
   Operator runs /gamerule stew-stacker-addon:stew_limit
                   |
                   v
   Mutates live world state & broadcasts S2C sync packet
```

---

## 5. JSON Configuration Schema

### File Location: `.minecraft/config/stew-stacker.json`
```json
{
  "configVersion": 1,
  "stewLimit": 16
}
```

| JSON Field | Type | Default | Rationale |
| :--- | :---: | :---: | :--- |
| `configVersion` | integer | `1` | Schema version tracking for backward-compatible migrations |
| `stewLimit` | integer | `16` | Baseline default stack limit for all stew and soup items |

---

## 6. Exhaustive Reference Matrix

| Identifier | Type | Default | Range | In-Game Command |
| :--- | :---: | :---: | :---: | :--- |
| `stew-stacker-addon:stew_limit` | Integer | `16` | $1 \dots 2{,}147{,}483{,}647$ | `/gamerule stew-stacker-addon:stew_limit <int>` |

### Localization Keys (`lang/en_us.json`):
* `config.stew-stacker-addon.title`: `Stew Stacker Addon`
* `config.stew-stacker-addon.category.general`: `General Settings`
* `config.stew-stacker-addon.group.categories`: `Stew Stacking Configuration`
* `config.stew-stacker-addon.option.stew_limit`: `Stew & Soup Limit`
* `config.stew-stacker-addon.option.stew_limit.description`: Limit for stews and beetroot soup. Default: 16...
* `config.stew-stacker-addon.option.warning`: ⚠️ WARNING: Do you really want to set the value to %d?...
* `gamerule.category.stew-stacker-addon.stew-stacker-addon`: `Stew Stacker Addon`
* `gamerule.stew-stacker-addon.stew_limit`: `Stew & Soup Limit`
* `gamerule.stew-stacker-addon.stew_limit.description`: Maximum stack size for stews and beetroot soup...

---

## 7. Developer & Config Hooks

### GameRule Registration with DasikLibrary (`StewStackerFabric.java`):
```java
STEW_LIMIT = DynamicGameRuleManager.integerRule(MOD_ID + ":stew_limit", CUSTOM_CATEGORY, StewStackerConfig.get().stewLimit)
    .name("Stew Limit")
    .description("Maximum stack size for stews and beetroot soup. Default: 16")
    .range(1, Integer.MAX_VALUE)
    .register();
```

### YACL Controller Builder (`YaclScreenHelper.java`):
```java
.controller(opt -> IntegerFieldControllerBuilder.create(opt)
    .min(1)
    .max(Integer.MAX_VALUE)
)
```

---

## 🔗 Related Documentation Links
* Return to [[Minecraft 26.2 Portal|26.2-Home]].
* Read [[26.2 Stew Stacking & Consumption Ergonomics|26.2-Stew-Stacking-and-Consumption-Ergonomics]].
* Read [[26.2 Network Synchronization & Payloads|26.2-Network-Synchronization-and-Payloads]].
* Read [[26.2 Architecture & Addon Hooks|26.2-Architecture-and-Addon-Hooks]].
